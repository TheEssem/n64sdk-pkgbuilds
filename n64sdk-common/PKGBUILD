# Maintainer: Essem <smswessem@gmail.com>

pkgname=n64sdk-common
pkgver=2.0L
pkgrel=1
pkgdesc="Include files used in both N64 SDK libraries"
url="https://github.com/CrashOveride95/n64sdkmod"
arch=('any')
license=('custom')
source=("git+https://github.com/CrashOveride95/n64sdkmod.git")
sha256sums=('SKIP')

package() {
  cp -dr --no-preserve='ownership,mode' "${srcdir}/n64sdkmod/packages/n64sdk-common/usr" "${pkgdir}"
}
